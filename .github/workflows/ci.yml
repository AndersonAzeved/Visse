name: CI/CD Visse

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    name: Build, Test & Analyze
    runs-on: ubuntu-latest

    # Define a pasta 'visse' como padrão para todos os comandos 'run' (npm install, etc)
    defaults:
      run:
        working-directory: ./visse

    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          # Importante: Diz ao setup-node onde está o package-lock.json
          cache-dependency-path: visse/package-lock.json

      - name: Instalar Dependências
        run: npm install

      - name: Executar Cobertura de Testes (Jest)
        run: npm run coverage

      - name: Análise do SonarCloud
        uses: SonarSource/sonarcloud-github-action@master
        with:
          # Importante: Diz ao SonarScanner para rodar dentro da pasta 'visse'
          projectBaseDir: visse
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}